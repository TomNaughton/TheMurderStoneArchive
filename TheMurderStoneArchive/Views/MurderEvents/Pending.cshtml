@model IEnumerable<TheMurderStoneArchive.Models.MurderEvent>

<div class="container mt-4">
    <h2>Pending Submissions Moderation Queue</h2>
    <table class="table table-striped mt-3">
        <thead>
            <tr>
                <th>Title</th>
                <th>Category</th>
                <th>Submitted Location</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.Title</td>
                    <td>@item.Category</td>
                    <td>@item.Location?.Name</td>
                    <td>
                        <form asp-action="Approve" asp-route-id="@item.Id" method="post" class="d-inline">
                            <button type="submit" class="btn btn-sm btn-success">Approve</button>
                        </form>
                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Edit First</a>
                        <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Reject</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>